{
  "name": "@doptor/backend-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix api",
    "build": "npm run build --prefix api",
    "lint": "npm run lint --prefix api"
  },
  "workspaces": [
    "api",
    "workers",
    "realtime"
  ]
}
